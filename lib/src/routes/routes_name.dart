part of 'routes_pages.dart';

abstract class RouteName {
  RouteName._();
  static const root = '/'; //route 'root' is default for homepage
  static const firstPage = '/firstpage';
  static const secondPage = '/secondpage';
  static const counter = '/counter';
  static const counterTanpaBlockProvider = '/counterTanpaBlockProvider';
}
